/* Google Analytics 4 stub - loaded on every page.
   The gtag.js library is injected dynamically by main.js after the user
   accepts cookies. Calls to gtag() before consent are queued in dataLayer
   but NOT sent to Google. See privacy.html for details. */
window.dataLayer = window.dataLayer || [];
function gtag(){dataLayer.push(arguments);}
